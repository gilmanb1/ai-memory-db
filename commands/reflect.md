---
description: Ask a question and get a synthesized answer from memory. The reflect agent searches observations, then raw facts, then synthesizes an answer.
---

!env MEMORY_TEXT="$ARGUMENTS" python3 ~/.claude/hooks/reflect_cmd.py
