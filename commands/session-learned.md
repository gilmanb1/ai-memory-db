---
description: Show what was learned from a session. Usage: /session-learned [session_id] [--limit N]. Without session_id, shows the most recent session.
---

!cd ~/.claude && python3 -m memory session-learned $ARGUMENTS
